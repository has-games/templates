services:
  {{ .StackName }}:
    image: {{ .Image }}:{{ .Tag }}
    container_name: hasgames-{{ .StackName }}
    environment:
      EULA: "TRUE"
      MEMORY: "{{ .Env.MEMORY }}"
    ports:
      - "25565:25565"
    volumes:
      - ./data:/data
    restart: unless-stopped
