# Has Games — game-server templates

Public catalog of game-server templates used by the [Has Games](https://github.com/has-games) bot.

Each subdirectory under `templates/` is one game-server template: a `template.yaml`
manifest and a `compose.yaml.tmpl` Docker Compose file.
